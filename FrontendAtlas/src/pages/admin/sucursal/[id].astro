---
import Layout from "../../../layouts/Layout.astro";
import SucursalAdminView from "../../../components/admin/sucursal/SucursalAdminView";

// Get ID
const { id } = Astro.params;
const sucursalId = Number(id);
---

<Layout title="Administración de Sucursal - Atlas">
  <SucursalAdminView client:only="react" sucursalId={sucursalId} />
</Layout>
